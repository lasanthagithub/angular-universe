import { AppUniverseModule } from './app-universe.module';
import {} from 'jasmine';
describe('AppUniverseModule', () => {
  it('should work', () => {
    expect(new AppUniverseModule()).toBeDefined();
  });
});
